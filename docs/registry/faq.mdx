---
title: 常见问题
sidebarTitle: 常见问题
---

<Note>
  MCP 注册表当前处于预览阶段。在正式发布之前可能发生破坏性变更或数据重置。如果您遇到任何问题，请在 [GitHub](https://github.com/modelcontextprotocol/registry/issues) 上报告。
</Note>

## 一般问题

### "官方 MCP 注册表"、"MCP 注册表 API"、"MCP registry"等有什么区别？

- "MCP Registry API"（MCP 注册表 API）——实现 MCP 注册表定义的 [OpenAPI 规范](https://github.com/modelcontextprotocol/registry/blob/main/docs/reference/api/openapi.yaml)的 API。
- "Official MCP Registry API"（官方 MCP 注册表 API）——在 `https://registry.modelcontextprotocol.io` 提供的 REST API，是 MCP 注册表 API 的超集。其 OpenAPI 规范可以从 [https://registry.modelcontextprotocol.io/openapi.yaml](https://registry.modelcontextprotocol.io/openapi.yaml) 下载。
- "MCP registry"（MCP 注册表实例）——提供 MCP 注册表 API 的第三方服务。
- "Official MCP Registry"（官方 MCP 注册表，或 "The MCP Registry"）——位于 `https://registry.modelcontextprotocol.io` 的服务。

### 我可以删除/取消发布我的服务器吗？

目前不可以。截至撰写本文时，有[公开讨论](https://github.com/modelcontextprotocol/registry/issues/104)。

### 如何更新我的服务器元数据？

提交一个带有唯一版本字符串的新 `server.json`。一旦发布，版本元数据是不可变的（类似于 npm）。

### 我可以在发布时添加自定义元数据吗？

可以，`_meta.io.modelcontextprotocol.registry/publisher-provided` 下的自定义元数据在发布到注册表时会被保留。这允许您包含特定于发布过程的自定义元数据。

<Warning>

有 4KB 的大小限制（4096 字节的 JSON）。如果超过此限制，发布将失败。

</Warning>

## 报告问题

### 如果我需要报告垃圾或恶意服务器怎么办？

1. 向底层包注册表（例如 NPM、PyPI、DockerHub 等）报告为滥用；以及
2. 在注册表仓库上创建一个标题以 `Abuse report: ` 开头的 GitHub issue。

### 如果我需要报告注册表本身的安全漏洞怎么办？

请遵循 [MCP 社区 SECURITY.md](https://github.com/modelcontextprotocol/.github/blob/main/SECURITY.md)。
