{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf docs/.mintlify/skills && mkdir -p docs/.mintlify/skills public && cp -RL plugins/mcp-spec/skills/. docs/.mintlify/skills/ && cd docs && npx mint export --output ../public/docs.zip && cd ../public && unzip -q docs.zip && rm docs.zip",
  "outputDirectory": "public",
  "cleanUrls": true
}
